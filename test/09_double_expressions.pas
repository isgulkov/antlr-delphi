
var
a, b : double;
begin
	

	a := 10.0 - 5.0;
	writeln('10.0 - 5.0 equals...');
	writeln(a);

	b := 1.0 / 0.5 + 100.0001;
	writeln('1.0 / 0.5 + 100.0001 equals...');
	writeln(b);

	writeln('(1.0 - 1.0 + 1.0) / 2.0 equals...');
	writeln((1.0 - 1.0 + 1.0) / 2.0);

end.

